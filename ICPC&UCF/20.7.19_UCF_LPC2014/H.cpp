#include <cstring>
#include <iostream>
using namespace std;
bool WasMark[16];
int main() {
  int T, n, m, a, s, x;
  cin >> T;
  cin >> T;
  for (int c = 1; c <= T; c++) {
    cin >> n >> m;
    for (int i = 0; i < m; i++) {
      cin >> a >> s >> x;
      
    }

    cout << "Case #" << c << ": ";
  }
  return 0;
}
