// 题目链接：https://www.51nod.com/Challenge/Problem.html#problemId=3198
#include <iostream>
#define BT ios::sync_with_stdio(false), std::cin.tie(0), std::cout.tie(0);
using namespace std;
struct node {
  string names;
  int fraction;
} arr[10010];
bool cmp(node a,node b){
  return a.fraction
}
int main() {
  BT int n;
  for (int i = 0; i < n; i++) cin >> arr[i].names >> arr[i].fraction;
  sort(arr, arr + n, arr->fraction);

  return 0;
}